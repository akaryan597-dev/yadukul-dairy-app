
import { useContext } from 'react';
// The context is defined in AuthContext.tsx, so we re-export the hook from there.
// This file is kept for structural consistency.
export { useAuth } from '../contexts/AuthContext';
