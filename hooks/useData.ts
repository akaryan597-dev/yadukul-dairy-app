
import { useContext } from 'react';
// The context is defined in DataContext.tsx, so we re-export the hook from there.
// This file is kept for structural consistency.
export { useData } from '../contexts/DataContext';
